export declare function promptRoutePath(paths: string[]): Promise<{
    url: string;
}>;
export declare function promptRouteMethodType(types: string[]): Promise<{
    type: string;
}>;
export declare function promptRouteMethodOverride(overrides: string[]): Promise<{
    name: string;
}>;
export declare function promptProxy(proxies: string[]): Promise<{
    proxy: string;
}>;
