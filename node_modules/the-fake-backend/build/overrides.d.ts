import { Route, Override } from './interfaces';
import { RouteManager } from './routes';
import { FileStorage } from './storage';
import { Middleware } from './types';
export declare class OverrideManager {
    private routeManager;
    private fileStorage?;
    constructor(routeManager: RouteManager, fileStorage?: FileStorage<"overrides"> | undefined);
    applyExternalOverrides(): void;
    getAll(): Route[];
    getAllSelected(): Override[];
    choose(): Promise<Override>;
    createOverriddenRouteMethodMiddleware(): Middleware;
    createOverriddenRouteContentMiddleware(): Middleware;
}
