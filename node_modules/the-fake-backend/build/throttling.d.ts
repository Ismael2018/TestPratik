import { Throttling } from './interfaces';
import { Middleware } from './types';
export declare class ThrottlingManager {
    private currentThrottlingIndex;
    private throttlings;
    constructor(throttlings?: Throttling[]);
    private resolveRouteDelay;
    getAll(): Throttling[];
    getCurrent(): Throttling | null;
    getCurrentDelay(): number;
    toggleCurrent(): void;
    createMiddleware(): Middleware;
}
